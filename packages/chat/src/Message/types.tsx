export interface IMessageProps {
  image?: string;
  authorName?: string;
  time?: string;
  text?: string;
  isRightDir?: boolean;
  showTitle?: boolean;
  showAvatar?: boolean;
  avatar?: string;
}
